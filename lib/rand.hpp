#pragma once

#include <string>

namespace thistlethwaite {
	/**
	 * Returns a specified number of random rotations.
	 * @param n the number of rotations.
	 * @return a string representing the specified number of random rotations.
	 */
	std::string get_random_rotations(const int &n);
}